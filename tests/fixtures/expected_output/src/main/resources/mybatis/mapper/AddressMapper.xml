<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexacro.uiadapter.mapper.AddressMapper">

  <select id="select_address_datalist_map" parameterType="java.util.Map"
          resultType="java.util.Map">
    SELECT ADDRESS_ID, CUSTOMER_ID, LINE1
      FROM TB_ADDRESS
     WHERE 1=1

    <if test="address_id != null and address_id != ''"> AND ADDRESS_ID = #{address_id}</if>

    <if test="customer_id != null and customer_id != ''"> AND CUSTOMER_ID = #{customer_id}</if>

    <if test="line1 != null and line1 != ''"> AND LINE1 = #{line1}</if>

  </select>

  <insert id="insert_address_map" parameterType="java.util.Map">
    INSERT INTO TB_ADDRESS (ADDRESS_ID, CUSTOMER_ID, LINE1)
    VALUES (#{address_id}, #{customer_id}, #{line1})
  </insert>

  <update id="update_address_map" parameterType="java.util.Map">
    UPDATE TB_ADDRESS
       SET CUSTOMER_ID = #{customer_id},
           LINE1 = #{line1}
     WHERE ADDRESS_ID = #{address_id}
  </update>

  <delete id="delete_address_map" parameterType="java.util.Map">
    DELETE FROM TB_ADDRESS WHERE ADDRESS_ID = #{address_id}
  </delete>



</mapper>
